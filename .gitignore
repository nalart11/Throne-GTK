# Сборочные каталоги: в build/ лежат готовые бинарники и libcronet.so,
# в target/ — промежуточные файлы cargo. И то и другое собирается заново.
/build/
/target/
/core/build/

# Локальные данные и заметки
*.db
*.db-shm
*.db-wal
*.log
