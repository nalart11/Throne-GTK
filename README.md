# Throne GTK

Форк [Throne](https://github.com/throneproj/Throne) с интерфейсом на GTK4 и
libadwaita вместо Qt. Ядро — то же самое: sing-box 1.13 и Xray, собранные из
исходников Throne.

## Что это

Клиент прокси для Linux. Поддерживает VLESS (включая Reality и XTLS Vision),
VMess, Trojan, Shadowsocks, Hysteria2, TUIC, AnyTLS, Naive, SOCKS и HTTP;
профили с транспортом XHTTP исполняются Xray через локальный мост, всё
остальное — sing-box. Работает в двух режимах: локальный прокси на порту или
VPN через TUN-интерфейс.

Умеет: подписки (список ссылок, base64, Clash-YAML, JSON sing-box), проверку
задержек пачкой, замер скорости отдельного сервера, автовыбор лучшего сервера
группы, правила маршрутизации по доменам, адресам, портам, процессам и готовым
спискам geosite/geoip.

Интерфейс следует светлой и тёмной схеме рабочего стола, как любое приложение
на libadwaita, и переключается вместе с ней без перезапуска.

## Как это устроено

```
throne-gtk (Rust, GTK4)                 throne-gtk-core (Go)
  ├─ crates/app      интерфейс            sing-box 1.13.16
  ├─ crates/store    SQLite: профили      Xray-core 26.7.28
  ├─ crates/config   конфиг sing-box
  └─ crates/ipc      связь с ядром  ─────► unix-сокет, protobuf
```

Ядро не слушает сокет само: оно подключается к сокету интерфейса и проверяет,
что родительский процесс — именно `throne-gtk` и лежит в той же папке. Поэтому
`throne-gtk` и `throne-gtk-core` должны находиться рядом.

Профили хранятся ровно в том виде, в каком их ждёт ядро: outbound sing-box или
outbound Xray. Промежуточного представления протоколов нет — новый протокол
sing-box начинает работать без изменений в коде.

## Сборка

Нужны: Rust, Go, protoc, GTK4 ≥ 4.12, libadwaita ≥ 1.5.

Ядро собирается Go 1.26: в Go 1.27 линковка падает на `//go:linkname`-хаках
sing-box и sing-trusttunnel к внутренностям `golang.org/x/net/http2` (нужный
символ теперь инлайнится). `just build` выставляет `GOTOOLCHAIN=go1.26.0` сам,
тулчейн 1.26 докачивается автоматически.

```sh
just build          # ядро и интерфейс
just run            # отладочный запуск
just test           # тесты
```

Без `just`:

```sh
cd core/gen && protoc -I . --go_out=. --go-grpc_out=. libcore.proto && cd ../..
cd core && GOTOOLCHAIN=go1.26.0 CGO_ENABLED=1 go build -o ../build/throne-gtk-core -trimpath \
    -tags "with_clash_api,with_gvisor,with_quic,with_wireguard,with_utls,with_dhcp,with_tailscale,badlinkname,tfogo_checklinkname0" \
    -ldflags "-w -s -checklinkname=0" && cd ..
cargo build --release -p throne-app && cp target/release/throne-gtk build/
```

## Установка

```sh
just install        # или ./scripts/install.sh
```

Приложение появится в списке программ рабочего стола под именем «Throne GTK».
Файлы кладутся в `~/.local/lib/throne-gtk`, ярлык — в
`~/.local/share/applications`, иконки — в `~/.local/share/icons`. Права root не
нужны. Удалить: `just uninstall`.

## Режим VPN

TUN-интерфейс требует прав. Один раз:

```sh
just grant-tun      # setcap cap_net_admin,cap_net_raw+ep build/throne-gtk-core
```

Без этого режим VPN откажется включаться и скажет, какую команду выполнить.
Режим «Прокси» прав не требует.

## Данные

| Что | Где |
| --- | --- |
| Профили и настройки | `~/.config/throne-gtk/throne-gtk.db` |
| Кэш ядра | `~/.config/throne-gtk/cache.db` |
| Сокет связи с ядром | `$XDG_RUNTIME_DIR/throne-gtk/` |

Оригинальный Throne использует свою базу и не затрагивается. При первом запуске,
если его база найдена, предлагается перенести серверы, подписки и правила
маршрутизации; то же есть в меню («Импорт из Throne») и в терминале:

```sh
cargo run -p throne-app --example selftest -- --seed ~/.config/throne-gtk/throne-gtk.db
```

Правила переносятся и отдельно — кнопкой в шапке окна «Правила маршрутизации»:
серверы к тому моменту обычно уже на месте, а полный импорт добавил бы к ним
вторые копии групп. Правило переносится со всеми своими условиями и получает
режим «подошло любое». Не переносятся служебные правила (перехват DNS ядро
ставит само), цепочки на конкретный сервер и условия, которых нет в модели:
по источнику соединения, по сети и с инверсией — о них сообщает счётчик
пропущенных.

## Проверка на своих данных

`just selftest` импортирует базу Throne в память, собирает конфиг для каждого
протокола и отдаёт его ядру на проверку — включая конфиг автовыбора и по одному
синтетическому профилю на каждый заявленный протокол. Флаги: `--connect`
поднимает соединение и делает через него запрос, `--speed` замеряет скорость,
`--latency` прогоняет пакетную проверку задержек.

```
живая проверка соединения:
  vless        🇩🇪 Germany #1 — 204 No Content за 708 мс
               трафик: ↓ 3623 Б, ↑ 569 Б
```

## Автовыбор сервера

Первой строкой в группе стоит «Автовыбор». Под ним ядро само держит лучший
сервер: проверяет участников, уходит с упавшего и возвращается, когда тот
оживает. Известные задержки передаются ядру как начальное ранжирование, поэтому
первый выбор осмыслен ещё до первой проверки.

В шапке видно, какой сервер выбран сейчас и сколько участников живо. Правой
кнопкой по серверу — «Закрепить в автовыборе»; по строке «Автовыбор» — снять
закрепление. «Перепроверить автовыбор» в меню запускает полный обход сразу.

## Правила маршрутизации

Меню → «Правила маршрутизации» (Ctrl+M). Условие — домен и поддомены, домен
целиком, часть домена, выражение, адрес или подсеть, порт, процесс, готовый
список `geosite-*` / `geoip-*`. Действие — через прокси, напрямую, блокировать.
Порядок в списке задаёт приоритет: применяется первое подошедшее правило.
Списки geosite и geoip объявляются автоматически и качаются через зеркало
jsDelivr: ядро не стартует, пока не соберёт все `rule_set`, а обращаться к
GitHub напрямую можно не отовсюду.

Условий у правила может быть несколько — кнопка «+» в шапке списка условий.
Тогда правило спрашивает, когда срабатывать: при любом совпадении (список
исключений) или при всех сразу («этот процесс и только на этом порту»). Само
по себе перечисление разных признаков в одном правиле sing-box понимает
по-своему — домены с подсетями через «или», остальное через «и», — поэтому
такое правило собирается в `logical` с явно указанным режимом.

Для случаев, которые формой не выражаются, там же есть «Правила текстом» —
фрагмент конфига sing-box, вставляемый после правил из формы.

## Значок в трее

Закрытие окна прячет программу в значок: соединение продолжает работать, окно
возвращается щелчком по значку или запуском из списка программ. В меню значка
— «Открыть окно» и «Выйти»; выход гасит ядро и снимает туннель. Подсказка
значка показывает, к какому серверу подключены.

Значок работает по протоколу StatusNotifierItem, то есть его рисует панель
рабочего стола (quickshell, waybar, KDE). Если хоста значков в системе нет,
закрытие окна выключает программу, как раньше, — прятаться было бы некуда.
Оба переключателя — «Прятать в значок при закрытии» и «Запускать в значке» —
в настройках, страница «Соединение», группа «Окно».

## Чем отличается от оригинала

- Интерфейс на GTK4/libadwaita: следует теме системы, работает на Wayland без
  прослойки Qt.
- Свой генератор конфига (см. `crates/config/src/generate.rs`), а не порт
  2500 строк C++.
- Ядро собирается из исходников Throne с одним изменением: проверка родителя
  ждёт `throne-gtk`.
- Выхлоп naive работает через динамическую загрузку `libcronet.so`: статическая
  библиотека из поставки cronet-go содержит объекты сразу под несколько
  архитектур, и GNU ld отказывается её читать. Скрипт `scripts/fetch_cronet.sh`
  кладёт `.so` рядом с ядром.
- Не поддержаны: цепочки прокси, Tailscale и SSH-выхлопы, встроенная панель
  Clash.

## Лицензия

GPL-3.0-or-later, как у оригинального Throne.
